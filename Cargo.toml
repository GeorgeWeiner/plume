[package]
name = "plume"
version = "0.1.0"
edition = "2021"
description = "Plume — a feather-light terminal IDE prototype"
license = "MIT"

[dependencies]
portable-pty = "0.9.0"
ratatui = "0.30"
vt100 = "0.16"

[profile.release]
lto = true
strip = true
